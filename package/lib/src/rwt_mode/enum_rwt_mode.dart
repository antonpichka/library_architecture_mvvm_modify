/// What kind of mod will it be ?
/// Where to use ? - use in 'RWTMode','NamedVM' classes
enum EnumRWTMode {
  /// Release mod
  /// Where to use ? - use in 'RWTMode','NamedVM' classes
  release,

  /// Test mod
  /// Where to use ? - use in 'RWTMode','NamedVM' classes
  test;

  /// No initialize the parameters
  /// Where to use ? - use in 'RWTMode','NamedVM' classes
  const EnumRWTMode();
}