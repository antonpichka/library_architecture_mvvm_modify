name: library_architecture_mvvm_modify
description: MVVM Modify
version: 0.0.1
homepage: "Library Architecture MVVM modification"

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  sqflite: ^2.0.0
  shared_preferences: ^2.0.8
  path: ^1.8.0
  path_provider: ^2.0.0
  ansicolor: ^2.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:

